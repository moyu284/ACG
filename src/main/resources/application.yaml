server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/acgbbs?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  security:
    user:
      name: admin
      password: admin
mybatis-plus:
  type-aliases-package: xyz.moyuzhe.entity
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl